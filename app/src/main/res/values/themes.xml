<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Tel2What" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Light Mode Defaults -->
        <item name="android:windowBackground">@color/background_light</item>
        <item name="android:colorBackground">@color/background_light</item>
        <item name="colorSurface">@color/surface_light</item>
        <item name="colorOnSurface">@color/text_main_light</item>
        <item name="colorOnBackground">@color/text_main_light</item>

        <item name="android:textColorPrimary">@color/text_main_light</item>
        <item name="android:textColorSecondary">@color/text_sec_light</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/background_light</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>